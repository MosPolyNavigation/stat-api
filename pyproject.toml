[project]
name = "stat-api"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "aiofiles",
    "alembic",
    "fastapi-pagination",
    "fastapi",
    "httpx",
    "python-dotenv",
    "python-multipart",
    "sqlalchemy",
    "uuid",
    "uvicorn",
    "pydantic-settings",
    "apscheduler",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
lint = [
    "flake8>=7.2.0",
]
